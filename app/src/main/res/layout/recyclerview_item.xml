<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
                                                   xmlns:app="http://schemas.android.com/apk/res-auto"
                                                   android:layout_width="match_parent"
                                                   android:layout_height="wrap_content">

    <TextView
            android:id="@+id/textView_recyclerview_item_left"
            android:text=""
            android:lines="1"
            android:maxLines="1"
            android:textStyle="italic"
            android:singleLine="true"
            android:textColor="@color/colorListText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toLeftOf="@+id/textView_recyclerview_item_right"
            app:layout_constraintHorizontal_chainStyle="spread"
            android:padding="5dp"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
    />

    <TextView
            android:id="@+id/textView_recyclerview_item_right"
            android:text=""
            android:lines="1"
            android:maxLines="1"
            android:textStyle="italic"
            android:singleLine="true"
            android:textColor="@color/colorListText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintLeft_toRightOf="@+id/textView_recyclerview_item_left"
            app:layout_constraintRight_toRightOf="parent"
            android:padding="5dp"
            android:textAlignment="textEnd"/>

    <View
            android:id="@+id/view_stroke"
            android:layout_width="fill_parent"
            android:layout_height="1dp"
            android:background="@color/colorLine"
            android:layout_marginTop="0dp"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/textView_recyclerview_item_left"/>

</androidx.constraintlayout.widget.ConstraintLayout>